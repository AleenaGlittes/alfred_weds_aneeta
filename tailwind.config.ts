import type { Config } from "tailwindcss";

const config: Config = {
  theme: {
    extend: {
      fontFamily: {
        greatvibes: ["var(--font-great-vibes)"],
      },
    },
  },
};

export default config;